# Отчёт по результатам анализа Kubernetes Audit Log

Анализ выполнен по журналу `audit.log` (8 973 событий), обработанному скриптом `scripts/analyze-audit.py`. Ниже приведены фактические наблюдения по ключевым сценариям из задания.

## Подозрительные события

### 1. Доступ к секретам

- **Кто:** `system:admin`, `system:apiserver`, `system:k3s-supervisor`, `system:kube-controller-manager`, `system:node:overturn`
- **Где:** запрошены секреты как кластерные (`namespace=null`), так и в `kube-system`
- **Действия:** регулярные `list/watch/get`, все завершились кодом 200
- **Комментарий:** журнал не содержит запросов от `system:serviceaccount:secure-ops:monitoring`; попытка симуляции была отклонена до API. Зафиксированные операции выполнялись только системными компонентами. Никаких следов несанкционированного чтения секретов не обнаружено, однако стоит контролировать объём системных запросов к secret'ам.

### 2. Привилегированные поды

- **Кто:** `system:admin`
- **Где:** namespace `audit-zone` (pod `pod-privileged`) и дважды `secure-ops` (pod `privileged-pod`)
- **Когда:** 2025‑10‑21T15:26:38Z, 15:35:06Z, 15:38:00Z
- **Почему критично:** `privileged: true` открывает полный доступ к хост-узлу, что нарушает требования PSP/PodSecurity. Все запросы прошли успешно, значит защитные политики не применялись.

### 3. Использование kubectl exec

- **Наблюдение:** событий с `verb=create` и `subresource=exec` в журнале нет. Скрипт симуляции либо не выполнил команду, либо API отклонил её до записи в аудит. Требуется отдельная проверка этого пути.

### 4. Создание RoleBinding с правами cluster-admin

- **Кто:** `system:admin`
- **Где:** namespace `secure-ops`
- **События:** два запроса на создание `RoleBinding` `escalate-binding`, выдана роль `cluster-admin` (таймстемпы 15:35:06Z и 15:38:00Z), оба завершились 201.
- **Риск:** мгновенная эскалация привилегий service account `secure-ops/monitoring` до полного администрирования кластера.

### 5. Удаление audit-policy.yaml

- **Кто:** `system:admin`
- **Ресурс:** `namespaces/audit-zone`
- **Когда:** 2025‑10‑21T15:26:54Z
- **Статус:** код 200 (успешно)
- **Комментарий:** факт удаления политики аудита в тестовом namespace подтверждён журналом. Необходимо мониторить подобные операции в реальной среде, т.к. они делают дальнейший аудит бессмысленным.

## Выводы
1. Привилегированные поды создавались без ограничений - политики безопасности не применяются.
2. RoleBinding с ролью `cluster-admin` создан успешно - RBAC позволяет эскалацию прав service account.
3. Политика аудита удалена с кодом 200 - конфигурация аудита не защищена.
4. Доступ к секретам фиксировался только от системных аккаунтов; попытки от `secure-ops/monitoring` в журнал не попали.
5. Событий `kubectl exec` не записано - необходимо дополнительно проверить этот сценариq.
